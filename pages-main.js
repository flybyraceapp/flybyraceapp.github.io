window.FBR_UI={selectedSeries:'flybyrace-kart-academy',message:null,filter:'all',calendarOffset:0};
window.FBR_PAGES={};
window.FBR_PAGES.overview=({E,C,stat,health,empty})=>{const s=E.state,n=E.nextEvent(),rank=E.getRank(),nextTest=C.licenseTests.find(x=>!s.licenses.includes(x.id)),exam=nextTest&&E.licenseEligibility(nextTest.id),academyEntries=s.entries.filter(e=>E.seriesById(e.seriesId)?.academy),academyRounds=academyEntries.reduce((sum,e)=>sum+(E.seriesById(e.seriesId)?.rounds.length||0),0),academyDone=academyEntries.reduce((sum,e)=>sum+e.roundIndex,0),academyProgress=s.academy.completed?100:academyRounds?Math.round(academyDone/academyRounds*100):0,due=n&&n.days===0;return `<section class="race-hero" style="--series-color:${n?C.disciplines[n.series.discipline].color:'#d8ff3e'}"><div class="race-hero-copy"><span class="eyebrow">${n&&n.series.academy?'DRIVER ACADEMY':due?'RACE DAY':'NEXT COMMITMENT'}</span><h2>${n?n.series.name:s.pendingLicenseTest?'LICENCE TEST READY':'CHOOSE YOUR ROOKIE ACADEMY'}</h2><p>${n?`Round ${n.round.round}/${n.round.total} · ${n.round.layout} · ${n.date}`:'Kart, formula, touring car and road-car schools are open.'}</p><div class="hero-actions">${n?`<button class="primary-action" data-action="series" data-id="${n.series.id}">VIEW EVENT</button>${due?`<button class="secondary-action" data-action="race" data-id="${n.series.id}">RACE CONTROL</button>`:''}`:'<button class="primary-action" data-route="competitions">BROWSE ACADEMIES</button>'}</div></div><div class="race-countdown"><small>${due?'SESSION':'STARTS IN'}</small><strong>${n?(due?'GO':Math.max(0,n.days)):rank.id}</strong><span>${n&&!due?'DAYS':'DRIVER RANK'}</span></div></section><section class="metrics-row">${stat('BALANCE',E.money(s.balance),s.balance?'Career funds':'Start from zero','acid')}${stat('DRIVER RANK',rank.id,rank.name)}${stat('RATING',s.rating,'Performance score')}${stat('SAFETY',s.safety+'/1000','Clean laps build trust','blue')}</section><div class="overview-grid"><section class="panel span-2"><div class="panel-heading"><div><span class="section-kicker">REGISTERED PROGRAMMES</span><h2>Your schedule</h2></div></div>${s.entries.map(e=>{const x=E.seriesById(e.seriesId),r=x?.rounds[e.roundIndex],day=E.eventDay(e);return x?`<button class="programme-row" data-action="series" data-id="${x.id}"><i style="background:${C.disciplines[x.discipline].color}"></i><span><small>${x.academy?'ACADEMY':x.license+' LICENCE'} · ${C.disciplines[x.discipline].name}</small><strong>${x.name}</strong></span><span><small>NEXT</small><strong>${r?E.dateFromSeasonDay(day):'COMPLETE'}</strong></span><span><small>POINTS</small><strong>${e.points}</strong></span><b>›</b></button>`:''}).join('')||empty('No registered programme','Choose an academy, register, then attend on the scheduled day.')}</section><section class="panel"><div class="panel-heading"><div><span class="section-kicker">FOUNDATION</span><h2>${s.academy.completed?'Academy complete':'Four rookie paths'}</h2></div><span class="license-orb" style="--license:${s.academy.completed?'#78e6b0':'#d8ff3e'}">${s.academy.completed?'✓':'A'}</span></div><div class="license-progress"><div><span>ASSESSMENT</span><strong>${academyProgress}%</strong></div><p><span style="width:${academyProgress}%"></span></p><small>${s.academy.completed?'Rookie test unlocked':'Kart · Formula · Touring · Road car'}</small><button class="text-button" data-route="competitions">CHOOSE ACADEMY ›</button></div></section><section class="panel"><div class="panel-heading"><div><span class="section-kicker">NEXT LICENCE</span><h2>${nextTest?nextTest.name:'All licences earned'}</h2></div></div><div class="team-mini">${nextTest?`<strong>${nextTest.id}</strong><span>SAFETY ${s.safety}/${nextTest.safety} · RATING ${s.rating}/${nextTest.rating}</span><p>${exam?.eligible?'You meet every application requirement.':exam?.reasons[0]||'Maximum licence reached'}</p><button class="text-button" data-route="driver">OPEN LICENSING ›</button>`:'<p>Professional licence ladder complete.</p>'}</div></section><section class="panel span-2"><div class="news-grid">${C.news.map(x=>`<article class="news-item"><small>${x.tag}</small><h3>${x.title}</h3><p>${x.summary}</p></article>`).join('')}</div></section><section class="panel"><div class="panel-heading"><h2>Garage</h2></div>${s.garage.length?s.garage.map(car=>{const avg=Math.round(Object.values(car.condition).reduce((a,b)=>a+b,0)/5);return `<div class="compact-car"><span>${car.name}</span><b class="${health(avg)}">${avg}%</b></div>`}).join(''):`<div class="empty-mini"><strong>NO VEHICLES</strong><span>Academies and early events provide or rent cars.</span><button class="text-button" data-route="garage">OPEN MARKET ›</button></div>`}</section></div>`};
window.FBR_PAGES.competitions=({E,C,stat})=>{let list=C.championships,filter=window.FBR_UI.filter;if(filter==='eligible')list=list.filter(x=>E.eligibility(x).eligible);else if(filter!=='all')list=list.filter(x=>x.license===filter||x.discipline===filter);const s=C.championships.find(x=>x.id===window.FBR_UI.selectedSeries)||list[0]||C.championships[0];window.FBR_UI.selectedSeries=s.id;const entry=E.entryFor(s.id),check=E.eligibility(s),round=s.rounds[entry?.roundIndex||0],day=entry&&E.eventDay(entry),days=entry?day-E.state.seasonDay:null,due=entry&&E.canRace(s.id),policy={provided:'PROVIDED CAR',rental:'RENTAL AVAILABLE',owned:'OWNER ENTRY',team:'TEAM CAR'}[s.vehiclePolicy]||'PROVIDED CAR',requirement=E.vehicleRequirementText(s);return `<div class="competition-toolbar"><div class="filter-pills"><button data-action="filter" data-id="all" class="${filter==='all'?'active':''}">ALL ${C.championships.length}</button><button data-action="filter" data-id="eligible" class="${filter==='eligible'?'active':''}">ELIGIBLE</button>${['ACADEMY','R','D','C','B','A','P'].map(x=>`<button data-action="filter" data-id="${x}" class="${filter===x?'active':''}">${x}</button>`).join('')}</div><select id="discipline-filter"><option value="all">All disciplines</option>${Object.entries(C.disciplines).map(([id,x])=>`<option value="${id}">${x.name}</option>`).join('')}</select></div><div class="competition-layout"><section class="series-browser">${list.map(x=>{const ok=E.eligibility(x).eligible,active=E.entryFor(x.id);return `<button class="series-card ${x.id===s.id?'selected':''} ${ok||active?'':'locked'}" data-action="select-series" data-id="${x.id}" style="--series:${C.disciplines[x.discipline].color}"><span class="series-tier">${x.academy?'A':x.license}</span><span><small>${C.disciplines[x.discipline].name.toUpperCase()} · ${x.rounds.length} ROUNDS</small><strong>${x.name}</strong><p>${x.className} · ${x.vehiclePolicy.toUpperCase()}</p></span><span class="series-prize"><small>${x.academy?'TUITION':'ENTRY FEE'}</small><strong>${x.academy?'FREE':E.money(E.entryCost(x))}</strong><b class="${active?'':'open'}">${active?active.status.toUpperCase():ok?'REGISTRATION OPEN':'LOCKED'}</b></span></button>`}).join('')}</section><aside class="series-detail panel" style="--series:${C.disciplines[s.discipline].color}"><div class="series-detail-hero"><span>${s.academy?'ROOKIE ACADEMY':s.license+' LICENCE · '+C.disciplines[s.discipline].name}</span><h2>${s.name}</h2><p>${s.description}</p></div><div class="series-specs">${stat('ENTRY',entry?'REGISTERED':E.money(E.entryCost(s)),entry?(due?'RACE DAY':days+' days to go'):'Registration cost')}${stat('VEHICLE',policy,requirement)}${stat('RATING',s.ratingRequired||0,'Minimum')}${stat('SAFETY',s.safetyRequired||0,'Minimum')}</div>${!check.eligible&&!entry?`<div class="eligibility-block"><strong>ENTRY REQUIREMENTS</strong>${check.reasons.map(x=>`<p>× ${x}</p>`).join('')}</div>`:''}<div class="session-format"><span>P</span><b>PRACTICE</b><i>›</i><span>Q</span><b>QUALIFYING</b><i>›</i><span>R</span><b>RACE</b></div><div class="round-list">${s.rounds.map((r,i)=>{const rd=entry?.roundDays?.[i];return `<div class="round-row ${entry&&i===entry.roundIndex?'current':''} ${entry&&i<entry.roundIndex?'complete':''}"><span>${String(i+1).padStart(2,'0')}</span><strong>${r.layout}</strong><small>P ${r.practice}m · Q ${r.qualify}m · R ${E.sessionRules(r).raceMinutes}m</small><b>${entry&&Number.isFinite(rd)?E.dateFromSeasonDay(rd):r.weather[0]}</b></div>`}).join('')}</div><div class="series-actions">${entry&&entry.status!=='completed'?`${due?`<button class="primary-action" data-action="race" data-id="${s.id}">OPEN RACE CONTROL</button>`:`<button class="primary-action" disabled>${days>0?'REGISTERED · '+days+' DAYS TO GO':'EVENT MISSED'}</button>`}<button class="secondary-action" data-action="withdraw" data-id="${s.id}">CANCEL ENTRY</button>`:entry?'<button class="primary-action" disabled>PROGRAMME COMPLETE</button>':`<button class="primary-action" data-action="enter" data-id="${s.id}" ${check.eligible?'':'disabled'}>REGISTER FOR PROGRAMME</button>`}</div></aside></div>`};window.FBR_PAGES.inbox=({E,empty})=>{const list=E.state.inbox,m=list.find(x=>x.id===window.FBR_UI.message)||list[0];if(m)window.FBR_UI.message=m.id;return `<div class="inbox-layout"><section class="panel inbox-list">${list.map(x=>`<button class="message-row ${x.unread?'unread':''} ${m?.id===x.id?'selected':''}" data-action="message" data-id="${x.id}"><span class="message-type ${x.type}">${x.type[0].toUpperCase()}</span><span><small>${x.from} · ${x.date}</small><strong>${x.subject}</strong><p>${x.body.slice(0,78)}…</p></span></button>`).join('')}</section><section class="panel message-detail">${m?`<div class="message-meta"><span class="message-type ${m.type}">${m.type[0].toUpperCase()}</span><div><small>FROM ${m.from.toUpperCase()}</small><h2>${m.subject}</h2><span>${m.date} 2026</span></div></div><div class="message-body"><p>${m.body}</p>${m.type==='offer'&&E.state.teamOffers.length?`<div class="message-callout"><strong>CONTRACT AVAILABLE</strong><button class="primary-action" data-action="accept-team" data-id="${E.state.teamOffers[0]}">ACCEPT CONTRACT</button></div>`:''}</div><button class="secondary-action" data-action="archive" data-id="${m.id}">ARCHIVE</button>`:empty('Inbox clear','Advance time for new messages.')}</section></div>`};
window.FBR_PAGES.calendar=({E,C,stat})=>{const current=E.dateValue(),target=new Date(current.getFullYear(),current.getMonth()+window.FBR_UI.calendarOffset,1,12),year=target.getFullYear(),month=target.getMonth(),monthName=target.toLocaleDateString('en-US',{month:'long',year:'numeric'}).toUpperCase(),eventMap=new Map();E.state.entries.forEach(entry=>{const s=E.seriesById(entry.seriesId);s?.rounds.forEach((r,i)=>{const day=entry.roundDays?.[i];if(!Number.isFinite(day))return;const d=new Date(current);d.setDate(d.getDate()+day-E.state.seasonDay);if(d.getFullYear()!==year||d.getMonth()!==month)return;const key=d.getDate();if(!eventMap.has(key))eventMap.set(key,[]);eventMap.get(key).push({s,r,entry,complete:i<entry.roundIndex,current:i===entry.roundIndex})})});const mondayOffset=(new Date(year,month,1,12).getDay()+6)%7,daysInMonth=new Date(year,month+1,0).getDate(),previousDays=new Date(year,month,0).getDate(),cells=[];for(let i=0;i<42;i++){const raw=i-mondayOffset+1,day=raw<1?previousDays+raw:raw>daysInMonth?raw-daysInMonth:raw,inMonth=raw>=1&&raw<=daysInMonth,date=new Date(year,month+(raw<1?-1:raw>daysInMonth?1:0),day,12),isToday=date.toDateString()===current.toDateString(),events=inMonth?(eventMap.get(day)||[]):[];cells.push(`<div class="month-day ${inMonth?'':'outside'} ${isToday?'today':''}"><header><span>${day}</span>${isToday?'<b>TODAY</b>':''}</header><div>${events.slice(0,3).map(x=>`<button class="month-event entered ${x.complete?'complete':''}" style="--event:${C.disciplines[x.s.discipline].color}" data-action="series" data-id="${x.s.id}"><i></i><span><strong>${x.r.layout}</strong><small>${x.s.name}${x.current?' · NEXT':''}</small></span></button>`).join('')}${events.length>3?`<small class="more-events">+${events.length-3} MORE</small>`:''}</div></div>`)}return `<section class="calendar-toolbar">${stat('MONTH',monthName,E.formatLongDate(current),'acid')}${stat('ENTRIES',E.state.entries.filter(x=>x.status==='entered').length,'Active programmes')}${stat('WORKSHOP',E.state.repairs.length,'Active jobs')}${stat('EVENTS',[...eventMap.values()].flat().length,'Registered this month')}</section><div class="month-controls"><button data-action="calendar-prev">‹</button><h2>${monthName}</h2><button data-action="calendar-today">TODAY</button><button data-action="calendar-next">›</button></div><section class="month-calendar"><div class="month-weekdays">${['MON','TUE','WED','THU','FRI','SAT','SUN'].map(x=>`<span>${x}</span>`).join('')}</div><div class="month-grid">${cells.join('')}</div></section>`};